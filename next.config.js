module.exports = {
  reactStrictMode: true,
  // Enable static export mode
  output: 'export',
  // Optional: Set image optimization if needed
  images: {
    unoptimized: true, // Required for static export
  },
}